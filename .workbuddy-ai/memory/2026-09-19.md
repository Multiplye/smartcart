# 2026-09-19 — SmartCart

## Migrated frontend products into SQLite (handoff step 12)

**Goal:** move the ~30 demo products from `src/data/products.js` into the backend DB so
Flask becomes the single source of truth.

### Key paths (note the triple-nested folder)
- Backend: `backend/app.py`, DB at `backend/instance/smartcart.db`, venv at `backend/venv/`
- Frontend: `smartcart/smartcart/src/` (workspace root -> smartcart -> smartcart)
- Source data: `smartcart/smartcart/src/data/products.js` (30 products, no description field)

### What was done
1. **`backend/import_products.py`** (new) — reads `products.js` with a regex (no Node dep),
   wipes the Product table, inserts all 30. Idempotent: re-running gives the same 30 rows,
   never duplicates. Builds a placeholder `description` per category since products.js has none
   but the column is `nullable=False`. Defaults `stock=10`.
2. **`backend/app.py`** — added a `product_to_dict()` helper and reused it in GET + POST
   (removed the duplicated dict literals).
3. **`src/data/api.js`** (new) — single place holding `API_BASE_URL = "http://127.0.0.1:5000"`
   and `getProducts()`.
4. **`src/components/Products.jsx`** — now fetches from the API with `useEffect` +
   loading/error/empty states instead of importing static data.
5. **`src/components/ProductList.jsx`** — same, still slices first 4 for the home page.
6. **`src/App.css`** — added `.products-status` / `.products-error` styles.

`src/data/products.js` deliberately left on disk (unused now) as a backup reference.

### Verified
- `import_products.py`: found 30, imported 30 (Electronics 10 / Fashion 10 / Home 10)
- Live API returned 30 products; CORS header present for `http://localhost:5173`
- `vite build` succeeds; `eslint` clean on the 3 changed files
- DB persists on disk after restart

### Gotchas hit
- The first regex failed because the real file has the object brace *before* `id:` and
  `image:` wrapping to a new line. Matching from `id:` onward (with `re.DOTALL`) works.
- Debugging tip: the Flask **reloader child** serves requests, so `taskkill` on the PID from
  `netstat` can leave the port still answering. Verify with a real `socket.bind()`, not netstat.

### Next step
Auth (register/login, roles), then Product CRUD, Cart, Orders, Reviews/Ratings,
Scikit-learn recommendations, Admin, final frontend wiring.

---

## Bugfix: `npm run dev` -> "Missing script: dev"

**Cause:** there are TWO nested folders both named `smartcart`. The user ran
`npm run dev` in `smartcart/smartcart/`, which only contains a Tailwind-only
`package.json` (deps: `@tailwindcss/vite`, `tailwindcss`) with **no scripts**.
The real React app is in the innermost `smartcart/smartcart/smartcart/`.

- Wrong: `smartcart/smartcart/`          -> `npm error Missing script: "dev"`
- Right: `smartcart/smartcart/smartcart/` -> works

**Fix applied:** documented, not restructured (did not move folders — too risky
for the user's working setup). Added:
- `HOW-TO-RUN.md` at project root — exact copy-paste commands for both servers,
  a folder-layout diagram, an error/fix table.
- `smartcart/smartcart/README-WRONG-FOLDER.md` — signpost in the wrong folder.

**Verified working:** backend on 127.0.0.1:5000 (30 products, CORS OK),
frontend on localhost:5173, full stack fetch confirmed.

### Other gotchas found
- Vite 8 binds **IPv6 only** (`[::1]:5173`), so `127.0.0.1:5173` refuses the
  connection while `localhost:5173` works. Normal, not a bug — but confusing
  when probing with tools that resolve to IPv4.
- Flask `debug=True` runs a reloader **child process** that does the actual
  serving, so killing the netstat PID can leave the port answering. Verify a
  port is free with `socket.bind()`, not netstat.

---

## Bugfix 2: `npm run build` -> ENOENT (a second wrong folder)

The user then ran npm from the **project root** `C:\Users\ashim\Downloads\smartcart`
and hit:

```
npm error code ENOENT
npm error path C:\Users\ashim\Downloads\smartcart\package.json
npm error enoent Could not read package.json
```

Same root cause as bugfix 1, one level higher. There are **three** levels a user
might stand in, and only the innermost works:
1. `smartcart/`             -> ENOENT (no package.json at all)
2. `smartcart/smartcart/`   -> "Missing script: dev"
3. `smartcart/smartcart/smartcart/` -> ✅ works

**Fix applied — guard rails, not restructuring.** Created a harmless
`package.json` in folders 1 and 2. Both have `dev`/`build`/`start` scripts that
route to a `wrong-folder` script which prints the exact `cd` command and
`process.exit(1)`. Verified: both now print a clear "WRONG FOLDER" message
instead of a cryptic npm error, and folder 3 still builds and runs normally.

Also updated `HOW-TO-RUN.md` with both errors in the troubleshooting table.

---

## Bugfix 3: guard rail misfired — npm run inside `backend/`

User ran `npm run dev` from `smartcart\backend\` (prompt showed `(venv) PS ...\backend>`)
and got the **project-root** guard-rail message, which said "you are in the project
root" — inaccurate, since they were in `backend`.

**Why:** `backend/` had no `package.json`, so **npm walks UP the folder tree** and
found the root one, printing the root's message. Important npm behaviour to remember.

**Fix:** added `backend/package.json` with a distinct message that correctly says
"WRONG FOLDER AND WRONG TOOL - this is a Python project, use `python app.py`"
and gives the right relative path to the frontend.

**Error I made and corrected:** my first version of the backend message said
`cd ..\smartcart\smartcart\smartcart` (three levels). The correct relative path
from `backend/` is `cd ..\smartcart\smartcart` (two levels). Caught it by
actually testing the path with `cd` + checking for `vite.config.js` before
trusting it. Lesson: always verify a printed path, don't compute it mentally.

**Final verified state — 4 locations, guard rails correct in all:**
| Folder | `npm run dev` behaviour |
|---|---|
| `smartcart\` (root) | "WRONG FOLDER" + correct cd |
| `smartcart\backend\` | "WRONG FOLDER AND WRONG TOOL" + `python app.py` |
| `smartcart\smartcart\` | "WRONG FOLDER" + `cd smartcart` |
| `smartcart\smartcart\smartcart\` | ✅ Vite starts normally |

Backend confirmed serving 30 products; frontend confirmed starting on 5173.

---

## Feature: Step 1 — Authentication (User table + register/login)

User chose **simple session model** (not JWT) with the note "simple but unique
type". Passwords ARE properly hashed via werkzeug's `generate_password_hash`
(scrypt) — no extra install, Flask bundles it. Confirmed `password_hash` never
appears in any API response.

### Backend (`backend/app.py`)
- New `User` model: `id, name, email (unique), password_hash, role`
- `ROLES = ["buyer","seller","admin"]`, default `buyer`
- `POST /api/register` -> 201 | 400 (missing field / pw<6 / bad role) | 409 (dup email)
- `POST /api/login`    -> 200 | 400 | 401
- Identical error message for wrong-password and unknown-email (anti user-enumeration)
- Emails stored lowercased -> login is case-insensitive
- Added `user_to_dict()` helper which deliberately omits `password_hash`

### Frontend
- `src/context/` split into 3 files: `AuthProvider.jsx`, `authContextInstance.js`,
  `useAuth.js`. **Split was required by eslint `react-refresh/only-export-components`** —
  a file exporting both a component and a non-component breaks Fast Refresh.
- `src/data/api.js`: added `login()`, `register()`, and a shared `postJson()` helper
  that converts the backend's `{error}` responses into thrown JS Errors.
- `src/components/Auth.jsx`: rewired to the real API. Kept the original markup/CSS
  classes. Added role dropdown, confirm-password check, error/success banners,
  submitting state, and a logged-in view with Log Out.
- `src/main.jsx`: wrapped in `<AuthProvider>`.
- `src/App.jsx`: navbar shows `Hi, <first name>` when logged in; removed a
  pre-existing unused `heroImage` import.
- `src/App.css`: styled `select` to match inputs, plus `.auth-message`,
  `.auth-error`, `.auth-success`, `.auth-btn:disabled`.

### Verified
- `backend/test_auth.py` — **24/24 checks pass**. Cleans up its own accounts.
- Live HTTP test against 127.0.0.1:5000: register 201, login 200,
  bad login 401, duplicate 409, weak pw 400, CORS header present.
- `npm run build` passes; `npx eslint src/` fully clean (0 problems).
- DB tables now `['product','user']`; 30 products intact after migration.

### Deliberate decision to note in the report
Role is self-selected via the signup dropdown, so anyone can register as
"seller". Left open on purpose; to be locked down with the admin panel.
Documented in `AUTH-GUIDE.md` under Known Limitations.

### Next
Step 2 — enforce roles: sellers may create products, buyers may not.

---

## Setup: git initialized (project had NO version control)

User asked "how to check what update is done just now". Discovered the project was
**not a git repository**, so there was no way to see or undo changes. Checked
`git --version` (2.55.0) and global config (already set: Multiplye /
ashimlamsal811@gmail.com).

**Did:** `git init`, wrote `.gitignore`, made the first commit `e72b424`
(38 files, 8926 insertions). Added `GIT-BASICS.md`.

`.gitignore` deliberately excludes:
- `backend/venv/`, `node_modules/`, `dist/` (huge, regenerable)
- `backend/instance/*.db` — **holds real user data and password hashes**,
  must never be committed
- `__pycache__/`, `*.pyc`, `.env`, editor/OS files

Commit message style used: short subject line, then bullets grouped by
backend/frontend.

**Gotcha discovered while demoing:** using `git checkout backend/app.py` to revert
a test edit caused Flask's reloader to restart, which killed my background shell
(`task ucQUwn` reported status "failed"). The backend was genuinely down. Had to
restart it. Not a project problem — just an artefact of editing `app.py` while the
dev server watches it. Worth remembering: any edit to `app.py` restarts Flask.

Also: git warns `LF will be replaced by CRLF` on Windows. Harmless.

---

## Feature: Step 2 — Role enforcement + Product CRUD

Commit `3634734`. Docs: `SELLER-GUIDE.md`.

### Backend (`backend/app.py`)
- `roles_required(*allowed)` decorator using `functools.wraps`. Returns
  **401** when not logged in, **403** when logged in with the wrong role.
- `get_current_user()` reads the `X-User-Id` header and loads the User from the
  DB. **Role comes from the database, never the header** — so editing the header
  cannot invent permissions. Documented in SELLER-GUIDE.md.
- `read_product_payload(data, existing=None)` — ONE validation function shared by
  create and update. Supports partial updates (omitted fields keep current value).
  Validates: required name/description/category, numeric price, numeric stock,
  no negatives.
- New routes: `GET /api/products/<id>`, `PUT /api/products/<id>`,
  `DELETE /api/products/<id>`.
- `GET /api/products` gained `?category=` (ilike) and `?search=` (name OR
  description).
- Added `backend/requirements.txt` with pinned versions
  (Flask 3.1.3, flask-cors 6.0.5, Flask-SQLAlchemy 3.1.1, SQLAlchemy 2.0.54,
  Werkzeug 3.1.8).
- **`backend/test_products.py` — 45 checks, all pass.** Covers 401/403 for
  anonymous and buyer, seller+admin allowed, 7 validation cases, partial update
  not wiping other fields, 404s, filters, and that the 30 real products are
  untouched. Self-cleaning.
- `test_auth.py` still passes (24 checks). Total 69.

### Frontend
- `api.js` rewritten around a single `requestJson(path, {method, body})` helper.
  `buildHeaders()` attaches `X-User-Id` read from localStorage. Added
  `getProduct`, `createProduct`, `updateProduct`, `deleteProduct`; `getProducts`
  now takes a `{category, search}` filter object.
- New `src/components/ManageProducts.jsx` — seller page: add/edit/delete form,
  product list with Edit + Delete buttons, confirm() before delete, friendly
  "this area is for sellers" screen for buyers, "please log in" for visitors.
- `App.jsx`: `/manage` route + nav link shown only when `isSeller || isAdmin`.
- `App.css`: manage form, rows, `.products-success`, responsive stacking.

### Lint lesson (recurring, worth remembering)
`react-hooks/set-state-in-effect` fires when an effect calls setState
**synchronously**. Fix: wrap the async work in an IIFE inside the effect, and
derive display flags (`const showLoading = canManage && loading`) instead of
setting state on the "not allowed" branch. Do not reach for
`eslint-disable` — restructure instead.

### Next
Give products an `seller_id` owner column so sellers manage only their own
listings (currently seller A can edit seller B's product). Then per-user cart.







## Steps 7b and 8 (recommendations frontend + admin panel)

### Step 7b - recommendations frontend
- `api.js`: `getProductRecommendations(id, limit)`, `getRecommendations(limit)`.
  IMPORTANT shape correction: the backend returns `recommendations` (NOT `items`)
  and `based_on` is `"your order history"` or `"popularity"` (NOT `"history"`).
  Each item also carries `match_score`, `match_percent`, `text_similarity`.
- `RecommendationStrip.jsx` (new) - reusable card row, `variant="similar"|"personal"`.
- `Recommendations.jsx` (new) - `/recommendations`. Shows `based_on_products` chips
  ("Because you bought") and an explainer of TF-IDF + cosine similarity.
- `ProductDetail.jsx` - "You might also like" strip. Fetched in its OWN useEffect
  that swallows errors, so a recommender failure cannot break the product page.
- `App.jsx` - `/recommendations` route, "For You" nav link, hero AI button now navigates.
- Component props gotcha: `Stars` takes `value`, not `rating`; sizes are `sm`/`md`.

### Step 8 - admin panel
Backend routes (all `@roles_required("admin")`):
  GET    /api/admin/users
  PUT    /api/admin/users/<id>/role
  DELETE /api/admin/users/<id>
  GET    /api/admin/stats
  GET    /api/admin/products
Three safety rules: no self role change, no self delete, no removing the last admin.
User delete cleans up order items -> orders -> cart -> reviews in ONE transaction,
and sets their products' `seller_id = NULL` (shop-owned) instead of deleting them.
New helpers: `account_counts_for(user_ids)` (one grouped query, like
`rating_summary_for`), `admin_user_to_dict(user, order_count)`.

Frontend: `components/Admin.jsx` (stats cards, status bar chart, searchable table,
two-step delete confirm), `api.js` admin functions, `/admin` route, admin CSS.
Added shared `.page-wrapper` / `.page-head` shell CSS (used by both new pages).

### Test totals (all passing)
test_products 65 | test_cart 62 | test_orders 79 | test_reviews 61 |
test_recommendations 59 | test_admin 77  ==> 403 checks, 0 failures.

### GOTCHA worth remembering - live HTTP scripts on this machine
Running a Python `urllib` script against localhost fails with a confusing
"404 Not Found" HTML page. Two causes, both hit at once:
  1. A system proxy at 127.0.0.1:62042 is configured; urllib routes even
     localhost through it. Fix: `urllib.request.build_opener(urllib.request.ProxyHandler({}))`
     and call `OPENER.open(...)` directly (installing globally is unreliable).
  2. `localhost` resolves to IPv6 `::1` first and something else is listening
     on `[::1]:5000`. Use `127.0.0.1` explicitly - Flask binds that.
See the header comment in `backend/live_demo_admin.py`.

Also: a helper that builds `f"{BASE}{path}"` with `BASE=".../api"` turns
`call('GET', '/')` into `/api/`, which is a genuine 404. That was MY bug,
not the app's - always sanity-check the URL a helper actually builds.

## Step 9 - navigation bugs, and Step 10 - docs + test isolation

### Step 9: two real navigation bugs found by surveying the pages
1. The navbar only rendered inside `Home`. Every interior page
   (/products, /orders, /manage, /admin, /recommendations, /product/:id)
   had NO navigation at all - only the browser back button worked.
2. Section links were bare anchors (`href="#categories"`). Those sections
   only exist on the home page, so from any other page they did nothing.

Fixes: `components/Navbar.jsx` (section links become `#x` on home and
`/#x` elsewhere, via useLocation + useNavigate), `components/PageLayout.jsx`
(applied at the ROUTE level so a new page cannot forget the navbar), and
`components/ScrollManager.jsx` (scroll to top on nav; retries ~500ms to
resolve a #hash after the target page renders).

### Step 10: test isolation bug - IMPORTANT LESSON
Running the 7 suites ONCE looked green. Running them TWICE exposed that
`test_admin.py` and `test_recommendations.py` were **permanently draining
product #1's stock** (each run ordered a real catalogue product and never
restored it). Product 1 hit stock 0, which then made a DIFFERENT suite
fail - and it looked like a recommender bug when the cause was an earlier
suite's side effect.
Fix: both suites now create their OWN throwaway product, restore any stock
they consume, and `test_admin.py` asserts no catalogue product moved.
Rule: a test may create/destroy its own fixtures but must never damage the
data it is checking.
Also fixed a fragile assertion in `test_reviews.py` that assumed every
product except its own had zero reviews (false once seed data exists).

### Final verified state
- 430 assertions across 7 suites, run TWICE, zero drift.
- Suite counts: auth 24, products 65, cart 62, orders 79, reviews 63,
  recommendations 59, admin 78.
- `seed_demo_data.py` (password `demo1234`) creates 5 accounts:
  aarav/priya/bikash (buyers, different tastes), sunita (seller),
  admin@demo.smartcart (admin). Options: `--reset`.
- `demo_recommendations.py` proves personalisation: Aarav's list is
  electronics-heavy, Priya's is home-heavy (Coffee Maker 36%), Bikash's
  leads with fashion. Visitor vs admin (both no history) get the IDENTICAL
  popularity list - the control case.
- `PROJECT-REPORT.md` written at the repo root.

### Suggestion for future sessions
Seventeen `test_*.py` / `live_demo_*.py` scripts now sit in `backend/`.
They work, but a `run_all_tests.py` that runs them in order and prints a
combined total would be a nice convenience.

---

## Step 11 - three UX fixes from user feedback (commit 3a4609b)

User reported: "in about us the link is kind a mistaken if we press that it
go ta login page it does not go ta about us manage that and make a for you ai
recomendation inside a much better and can you plz keep search bar inside
products?"

### Fix 1 - About Us landed on the wrong content
`id="about"` sat on a three-card features strip with no explanation of the
product. Replaced with a real `about-section` (feature cards + a "How the
recommendations work" panel describing TF-IDF/cosine honestly).

**Found a worse bug while auditing anchors:** the navbar's Login link pointed
at `#auth` and NO element had that id, so it silently did nothing. Wrapped
`<Auth />` in `<div id="auth">`. All five targets now resolve: #home,
#categories, #cart, #about, #auth, #contact. Added `scroll-margin-top: 100px`
so the fixed navbar stops covering the target heading.

### Fix 2 - Products page had no search bar at all
`grep -n "search" components/Products.jsx` returned only `useSearchParams` and
`searchParams.get("category")` - no input existed. Rewrote the page.

**Architecture decision (keep this):**
- Search -> **server-side**. `?search=` already matches name OR description,
  which a client-side name filter could never do, and it scales.
- Category -> **client-side**. Exact match on a field already fetched.
- `searchInput` (typing) vs `query` (searched) are separate, or the list
  refetches per keystroke.
- The URL carries the term so results are linkable and Back behaves.

### Fix 3 - For You page was thin
Rewrote `Recommendations.jsx`: worded match badges (Strong/Good/Related/
Popular), star ratings, "Similar to X" reason line, sort toolbar, stats strip,
spinner, and a nudge banner for visitors with no history. Sorting is local
(max 12 items). Added a note explaining fallback items show no percentage,
because a popularity ranking compares nothing - better than inventing a number.

**Backend change:** `based_on_products` now sends `image` and `category`. The
chips render a thumbnail, so sending name alone left every chip with a broken
image. This was a SECOND bug the user had not reported.

### Keyed-state pattern (replaces setState-in-effect)
Lint rule `react-hooks/set-state-in-effect` fires on a synchronous setState in
an effect (hit it again here). Fix used, and worth reusing:

```js
const [state, setState] = useState({ key: urlValue, value: urlValue });
if (state.key !== urlValue) setState({ key: urlValue, value: urlValue });
const value = state.value;   // re-reads URL on render, no effect needed
```
Store the URL value the state was derived from as a `key`; when the URL changes
underneath, the key mismatches and state re-syncs during render - no cascading
render. Remember to set BOTH the value and its key in any updater, or the guard
immediately undoes the edit.

### Verified after the changes
- `npx eslint src/` clean, 0 problems
- `npm run build` built in 3.06s; CSS grew 2875 -> 4129 lines (43 kB)
- All three changed modules transform via the dev server (checked the
  transformed JS for the key identifiers, not just HTTP 200)
- Backend suites **430 passed, 0 failed**; catalogue unchanged (30 products,
  0 zero-stock) - the Step 10 isolation fix holding
- Live checks: `?search=watch` returns Smart Watch via a **description** match;
  user 1 (Aarav, 3 orders) gets `personalised: true` with 3 chips each carrying
  `image` + `category`; a visitor gets `personalised: false`, the popularity
  fallback, and only 4 items

### Environment gotchas reconfirmed this session
- **The backend venv is `backend/venv/Scripts/python.exe`** (Python 3.12). The
  managed runtime `C:\Users\ashim\.workbuddy-ai\binaries\python\...\3.13.12`
  has NO Flask - using it makes every suite report DID NOT COMPLETE with
  `ModuleNotFoundError: No module named 'flask'`. Always use the project venv.
- The system proxy `127.0.0.1:62042` intercepts `localhost:517x` and returns
  **502 upstream connect failed** because it tries IPv4 while Vite listens
  IPv6-only. Use `curl --noproxy '*' 'http://[::1]:PORT/...'`.
- `npm run dev &` from the Bash tool DIES when the shell exits. To keep a
  dev server up, use `run_in_background: true` with `--port N --strictPort`,
  otherwise it burns through 5173/5174/5175 on each attempt.

### Not done
Still no browser automation on Windows, so none of this has been visually
confirmed - correctness is inferred from clean transforms, a clean build, and
the live API payloads.
