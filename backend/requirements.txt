# SmartCart backend dependencies
#
# Install everything with:
#   pip install -r requirements.txt
#
# Versions are pinned so the project behaves the same on any machine.

Flask==3.1.3
Flask-CORS==6.0.5
Flask-SQLAlchemy==3.1.1
SQLAlchemy==2.0.54
Werkzeug==3.1.8
